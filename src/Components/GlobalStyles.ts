import { createGlobalStyle } from 'styled-components';

export const GlobalStyles = createGlobalStyle`
  body {
    background-color: #1F1F1F; // Set a light background color
    color: white; // Ensure text is readable with the background color
    margin: 0;
    padding: 0;
  }
`;
